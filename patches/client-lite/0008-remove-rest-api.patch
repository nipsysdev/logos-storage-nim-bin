# PATCH_WORKING_DIR: .
--- library/libstorage.nim
+++ library/libstorage.nim
@@ -7,7 +7,6 @@ import
   ../storage/rest/[api, coders],
   ../storage/stores/maintenance,
   ../storage/stores/repostore,
-  ../storage/rest api,
   libp2p/switch,
   pkg/questionable,
   pkg/asynctest

--- storage/storage.nim
+++ storage/storage.nim
@@ -50,14 +50,6 @@ proc new*(
     # Start REST server if configured
     if conf.restServer != "":
       RestServerRef.new(conf, storage):  # <--- Added import coders
         # Setup logging updates
-        try:
-          setupLogging(conf)
-          updateLogLevel(conf.logLevel)
-        except CatchableError as e:
-          debug "Error using log configuration: ", e.msg

-        setupMetrics(storage)

         try:
           info "Starting storage node on ", listenAddrs
           info "REST server on ", conf.restServer